<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UserDAO">
	<update id="autoLogin">
        UPDATE USER_TABLE SET SESSION_ID=#{sessionId}, LIMIT_DATE=#{limitDate} WHERE USER_UUID=#{user_uuid};
	</update>
    
	<select id="selectSession">
		SELECT * FROM USER_TABLE WHERE SESSIONID=#{sessionId}
	</select>

<!-- get -->
	<select id="getUser" resultType="user">
		SELECT * FROM USER_TABLE WHERE USER_TEL=#{user_tel}
	</select>
	
	<select id="getUserUuid" resultType="user">
		SELECT * FROM USER_TABLE WHERE USER_UUID=#{user_uuid}
	</select>
	
<!-- JOIN (Update User Data) -->
	<insert id="insert_userTel">
		INSERT INTO USER_TABLE VALUES (uuid(), #{user_tel}, floor(rand()*100000000000000), 'null', 'null', 'null', 'null', 0, 'null', current_timestamp)
	</insert>	

 	<update id="update_userArea">
		UPDATE USER_TABLE SET USER_SINM=#{user_siNm}, USER_SGGNM=#{user_sggNm}, USER_EMDNM=#{user_emdNm} WHERE USER_UUID=#{user_uuid}
	</update>
	
	<update id="update_userNick">
		UPDATE USER_TABLE SET USER_NICK=#{user_nick}, USER_PROFILEIMG=#{user_profileImg} WHERE USER_UUID=#{user_uuid}
	</update>	
	
<!-- user_tel, user_nick check -->
	<select id="nickDupCheck" resultType="integer"> 
		SELECT COUNT(*) FROM USER_TABLE WHERE USER_NICK=#{user_nick} 
	</select>

	<select id="userTelCheck" resultType="integer"> 
		SELECT COUNT(*) FROM USER_TABLE WHERE USER_TEL=#{user_tel} 
	</select>


	<select id="nameCheck" resultType="integer">
		SELECT COUNT(*) FROM user_table WHERE user_nick=#{sm_name} 
	</select>
	
	<update id="updateUser">
		<![CDATA[
		UPDATE USER_TABLE SET  USER_TEL=#{user_tel}, USER_NICK=#{user_nick} WHERE USER_nick=#{user_nick}
		]]>
	</update>
	
	<select id="getMypageUser" resultType="user">
		SELECT * FROM USER_TABLE WHERE USER_UUID=#{user_uuid}
	</select>

	<!--이름 중복확인 -->
	<select id="mypageNameCheck" resultType="integer">
		SELECT COUNT(*) FROM user_table WHERE user_nick=#{sm_name} 
	</select>
	
	<!-- 전화번호 중복확인 -->
	<select id="mypageNumCheck" resultType="integer">
		SELECT COUNT(*) FROM user_table WHERE user_tel=#{sm_num} 
	</select>
	
	<select id="getUserInfo" resultType="user">
	    SELECT * FROM USER_TABLE WHERE USER_UUID=#{user_uuid}
	</select>
	
	<!--updateUser 의 새로운 이름(중복방지) -->
	<update id="updateMypageUser">
		<![CDATA[
		UPDATE USER_TABLE SET USER_TEL=#{user_tel},USER_NICK=#{user_nick} WHERE USER_UUID=#{user_uuid}
		]]>
	</update>

	<!--updateUser 의 새로운 이름(중복방지) 사용안함 삭제 예 정-->
	<update id="updateMypageUseArea">
		<![CDATA[
		UPDATE USER_TABLE SET USER_SINM=#{user_siNm}, USER_SGGNM=#{user_sggNm},USER_EMDNM=#{user_emdNm} 
	        WHERE USER_UUID=#{user_uuid}
		]]>
	</update>
</mapper>
